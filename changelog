adicionar um formato:

- obtenha o número da categoria
- default.formindex[ indice_da_categoria ].append( novo_formato )

remover um formato:

- obtenha o número da categoria
- default.formindex[ indice_da_categoria ].remove( formato_a_remover )

checar o formato:

- obtenha a lista dos arquivos no diretório
- cheque se o arquivo tem pontos no nome
- separe o arquivo pelos pontos
- cheque se o formato do arquivo está na lista dos formatos = list(cf.default.formats.keys())

----bugfix-----
> é necessário uma lista de todos os formatos cadastrados para saber se o formato encontrado no arquivo está na lista dos que devem ser movidos,
 no estado atual, os formatos estão cadastrados em uma lista de listas.
